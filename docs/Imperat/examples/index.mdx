---
id: "examples-index"
title: "Examples"
sidebar_position: 11
---
import DocCard from '@theme/DocCard';
import { usePluginData } from '@docusaurus/plugin-content-docs/client';

# Examples

:::caution[WARNING]
Please make sure you have read all the other tutorials before reading this one !
:::

In this section, I will be guiding you through popular scenarios on the **`BUKKIT`** platform
We will be creating some popular command examples as below.

{(() => {
  const { docs } = usePluginData('docusaurus-plugin-content-docs');
  const pluginData = usePluginData('docusaurus-plugin-content-docs');
  const currentIndexDocId = 'examples-index'; // The ID of your index page

  const categoryDocs = Object.values(docs).filter(
    (doc) =>
      doc.id !== currentIndexDocId
  );

  return (
    <div className="row">
      {categoryDocs.map((doc) => (
        <article key={doc.id} className="col col--6 margin-bottom--lg">
          <DocCard item={doc} />
        </article>
      ))}
    </div>
  );
})()}

