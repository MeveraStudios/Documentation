---
sidebar_position: 20
title: Command Hierarchy Diagram
---

Suppose you have a root command `/manage` for a server management framework. This command has several subcommands and arguments, demonstrating advanced features and usages:-
- `/manage user add <username> <role> [--email <email>] [--notify]`
- `/manage user remove <username> [--force]`
- `/manage server restart <serverId> [--immediate]`
- `/manage server status <serverId>`
- `/manage backup create <path> [--full] [--encrypt <key>]`

**Example Input:**

- `/manage user add Alice admin --email alice@example.com --notify`
- `/manage user remove Bob --force`
- `/manage server restart 42 --immediate`
- `/manage backup create /backups/2024-06-01 --full --encrypt secretKey`

---

## Tree Visualization
Imperat creates a tree data structure for every command being registered.
Below is the visualization of the tree for the example above: 

```mermaid
%% CommandTree: N-ary tree of commands and arguments (all circles, default style)
flowchart TD
    Manage((manage))
    User((user))
    Server((server))
    Backup((backup))
    Add((add))
    Remove((remove))
    Restart((restart))
    Status((status))
    Create((create))
    Username((username))
    Role((role))
    Email((email))
    Notify((notify))
    Force((force))
    ServerId1((serverId))
    Immediate((immediate))
    ServerId2((serverId))
    Path((path))
    Full((full))
    Encrypt((encrypt_key))

    Manage --> User
    Manage --> Server
    Manage --> Backup
    User --> Add
    User --> Remove
    Add --> Username
    Add --> Role
    Add --> Email
    Add --> Notify
    Remove --> Username
    Remove --> Force
    Server --> Restart
    Server --> Status
    Restart --> ServerId1
    Restart --> Immediate
    Status --> ServerId2
    Backup --> Create
    Create --> Path
    Create --> Full
    Create --> Encrypt
```